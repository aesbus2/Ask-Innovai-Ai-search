# Streamlined requirements.txt for Ask InnovAI
# Version: 2.1.0 - Optimized for Digital Ocean App Platform

# Core web framework (required)
fastapi>=0.110.0
uvicorn[standard]>=0.27.1
python-dotenv>=1.0.1
python-multipart>=0.0.9

# HTTP client for API requests (required)
requests>=2.31.0

# AI and embeddings (required for your use case)
sentence-transformers>=3.0.0
torch>=2.0.0
numpy>=1.22.0

# OpenSearch for vector database (required)
opensearch-py>=2.4.0

# HTML parsing and text processing (required)
beautifulsoup4>=4.12.0

# System monitoring (optional but helpful)
psutil>=5.9.0

# REMOVED HEAVY/UNNECESSARY DEPENDENCIES:
# - langchain-core, langchain-openai, langchain-community (not used)
# - transformers (already included in sentence-transformers)
# - openai (not needed since you use Digital Ocean AI)
# - All CUDA-specific packages (will auto-install if needed)